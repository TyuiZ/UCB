#include <stdio.h>

int main(void)
{

float dados[100];
float calculo = 0;
int i;

for ( i=1; i <= 100; i++){
   printf("Digite o %d valor\n", i);
   scanf("%f\n", &dados[i]);
}
calculo += dados[i];

printf("Soma: %.2f\n", calculo);




    return 0;


}
